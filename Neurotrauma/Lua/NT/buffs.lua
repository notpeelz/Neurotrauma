local NTA = require("NT.afflictions")
local NTAffliction = NTA.Affliction

NTA.Register(NTAffliction({ id = "implacable" }))
NTA.Register(NTAffliction({ id = "cpr_buff" }))
NTA.Register(NTAffliction({ id = "psychosis" }))
